# Auto detect text files and perform LF normalization
* text=auto
# 忽略依赖包文件夹
node_modules/

# 忽略 npm 日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 根据情况决定是否忽略 lock 文件 (通常建议保留 lock 文件，但如果你是初学者且不需要版本锁定，也可以暂时忽略)
# package-lock.json