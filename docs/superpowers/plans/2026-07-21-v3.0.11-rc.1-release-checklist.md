# v3.0.11-rc.1 发版清单

**日期：** 2026-07-21  
**通道：** Pre-release（Latest 仍为 `v3.0.10`）  
**回滚：** `v3.0.10`

## 1. 发版合同

- [x] `package.json` → `3.0.11-rc.1`
- [x] `docs/CHANGELOG.md` 增加 `[3.0.11-rc.1]` 章节
- [x] `docs/RELEASE_POLICY.md` 版本线说明
- [x] `README.md` 最新发布表
- [x] `npm run release:validate`
- [x] `npm run release:gate` 各阶段（security→quality→coverage→tech-debt→build→artifact→smoke→performance；notes/package 在 clean commit 后补跑）
- [x] annotated tag `v3.0.11-rc.1`（本地）
- [ ] 推送 commit + tag 到 `sops`（触发 Release workflow，Pre-release）

## 2. 债务收口（本 RC）

| 项 | 状态 |
|----|------|
| CHANGELOG / 版本三者一致 | 已写 |
| deep-chat vendor 补丁构建 fail-closed | 已做 + 单测 |
| 生成中可切换 / 后台输出 / 未读 | 已实现 + 单测 |
| Skills 路由 smoke | 已加入 CORE_ROUTES |
| Skills 试用交接 smoke | 已加 E2E |
| 技能筛选跨页保留 | 已有（StorageService） |
| 软卸载保留在飞 LLM | 产品策略，CHANGELOG 已文档化 |
| controller 拆分 | P2 不阻塞 RC |
| 技能 eager 体积 | P2 不阻塞 RC；RC 构建后观察产物 |

## 3. 人工验收（RC 建议）

- [ ] 更多 → 技能：列表 / 搜索 / 分类 / 详情 / 复制
- [ ] 「在 Deep Chat 试用」：冷启动 + 已在 Deep Chat 时二次试用
- [ ] 新建会话 vs 附加当前；系统提示覆盖确认
- [ ] Chip / Context Bar 移除与撤销；刷新后仍可见
- [ ] 会话 A 生成中 → 切 B → A 后台完成 → 未读圆点 → 点回清除
- [ ] 输出中切走 → 后台完成
- [ ] 删除 / 清空「生成中」会话
- [ ] 无 LLM Key 错误提示
- [ ] 移动端技能页与 Deep Chat

## 4. 已知非阻塞

- 技能页无 CRUD（Phase A 只读）
- 技能紫 / Deep Chat 棕主题未完全统一
- `controller.ts` 上帝模块待后续拆分
