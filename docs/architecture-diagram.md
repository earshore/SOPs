# AI 分析加速架构图

## 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                         用户界面层                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │  产品选择    │  │  目标选择    │  │  性能设置    │         │
│  │  ASIN List   │  │  8 Targets   │  │  Settings    │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              开始分析按钮                                 │  │
│  │         [性能设置] [▶ 开始分析]                          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  实时进度显示                                            │  │
│  │  ████████████░░░░░░░░ 60%                               │  │
│  │  正在分析: target1, target2, target3 (6/8)              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      控制层 (Actions)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  runAnalysisAction()                                            │
│    ├─ 读取性能设置                                              │
│    ├─ 合并产品数据                                              │
│    └─ 调用并行分析服务                                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                   并行分析引擎 (核心)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  runParallelAIAnalysis()                                        │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  任务队列管理                                             │ │
│  │                                                           │ │
│  │  [Task1] [Task2] [Task3] [Task4] [Task5] [Task6] ...    │ │
│  │     ↓       ↓       ↓       ↓                            │ │
│  │  ┌─────────────────────────────────┐                     │ │
│  │  │   并发控制器 (maxConcurrency=4)  │                     │ │
│  │  │                                 │                     │ │
│  │  │   Slot 1: Task1 ████████ 100%  │                     │ │
│  │  │   Slot 2: Task2 ██████░░  75%  │                     │ │
│  │  │   Slot 3: Task3 ████░░░░  50%  │                     │ │
│  │  │   Slot 4: Task4 ██░░░░░░  25%  │                     │ │
│  │  │                                 │                     │ │
│  │  │   等待队列: [Task5, Task6, ...] │                     │ │
│  │  └─────────────────────────────────┘                     │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │  智能缓存层                                               │ │
│  │                                                           │ │
│  │  检查缓存 → 命中？                                        │ │
│  │     ├─ Yes → 返回缓存结果 (快速)                         │ │
│  │     └─ No  → 执行 LLM 分析 → 保存到缓存                  │ │
│  │                                                           │ │
│  │  缓存键: targetId + productHash + language               │ │
│  │  有效期: 24 小时                                          │ │
│  └───────────────────────────────────────────────────────────┘ │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      LLM 服务层                                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  callLLM()                                                      │
│    ├─ 构建提示词                                                │
│    ├─ 调用 API                                                  │
│    ├─ 重试机制 (最多2次)                                        │
│    └─ 解析 JSON 响应                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│                      外部 API                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  OpenAI / Claude / Gemini / 其他 LLM 提供商                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 数据流

### 串行方式（原方式）

```
开始
  ↓
Target 1 ████████████████████ (120s)
  ↓
Target 2 ████████████████████ (120s)
  ↓
Target 3 ████████████████████ (120s)
  ↓
Target 4 ████████████████████ (120s)
  ↓
Target 5 ████████████████████ (120s)
  ↓
Target 6 ████████████████████ (120s)
  ↓
Target 7 ████████████████████ (120s)
  ↓
Target 8 ████████████████████ (120s)
  ↓
完成 (总耗时: 960秒 = 16分钟)
```

### 并行方式（4并发）

```
开始
  ↓
批次 1 (并发执行)
  ├─ Target 1 ████████████████████ (120s)
  ├─ Target 2 ████████████████████ (120s)
  ├─ Target 3 ████████████████████ (120s)
  └─ Target 4 ████████████████████ (120s)
  ↓
批次 2 (并发执行)
  ├─ Target 5 ████████████████████ (120s)
  ├─ Target 6 ████████████████████ (120s)
  ├─ Target 7 ████████████████████ (120s)
  └─ Target 8 ████████████████████ (120s)
  ↓
完成 (总耗时: 240秒 = 4分钟)

加速比: 4x
```

### 并行方式 + 缓存（4并发，60%命中）

```
开始
  ↓
批次 1 (并发执行)
  ├─ Target 1 ████████████████████ (120s) - 新分析
  ├─ Target 2 █ (1s) - 缓存命中 ✓
  ├─ Target 3 ████████████████████ (120s) - 新分析
  └─ Target 4 █ (1s) - 缓存命中 ✓
  ↓
批次 2 (并发执行)
  ├─ Target 5 ████████████████████ (120s) - 新分析
  ├─ Target 6 █ (1s) - 缓存命中 ✓
  ├─ Target 7 █ (1s) - 缓存命中 ✓
  └─ Target 8 ████████████████████ (120s) - 新分析
  ↓
完成 (总耗时: 120秒 = 2分钟)

加速比: 8x
```

## 并发控制算法

```typescript
async function executeWithConcurrency(
  tasks: Task[],
  maxConcurrency: number
) {
  const runningTasks = new Set<Promise<void>>();
  
  for (const task of tasks) {
    // 等待直到有空闲槽位
    while (runningTasks.size >= maxConcurrency) {
      await Promise.race(runningTasks);
      // ↑ 等待任意一个任务完成
    }
    
    // 启动新任务
    const promise = executeTask(task)
      .finally(() => {
        runningTasks.delete(promise);
        // ↑ 任务完成后释放槽位
      });
    
    runningTasks.add(promise);
  }
  
  // 等待所有任务完成
  await Promise.all(runningTasks);
}
```

## 缓存机制

```
┌─────────────────────────────────────────────────────────────┐
│                      缓存决策树                              │
└─────────────────────────────────────────────────────────────┘

                    开始分析请求
                         ↓
                  生成缓存键
                         ↓
              cacheKey = hash(
                targetId,
                asin,
                productTitle,
                reviewCount,
                language
              )
                         ↓
                  查找缓存
                         ↓
                    存在？
                    ↙    ↘
                 Yes      No
                  ↓        ↓
              检查有效期   执行 LLM 分析
                  ↓        ↓
            < 24小时？    解析结果
              ↙    ↘       ↓
           Yes      No    保存到缓存
            ↓        ↓     ↓
        返回缓存   执行分析  返回结果
            ↓        ↓     ↓
            └────────┴─────┘
                     ↓
                  完成
```

## 失败处理

```
┌─────────────────────────────────────────────────────────────┐
│                    失败隔离机制                              │
└─────────────────────────────────────────────────────────────┘

并发执行 8 个任务
  ├─ Task 1 ✓ 成功
  ├─ Task 2 ✓ 成功
  ├─ Task 3 ✗ 失败 ← 记录错误，继续执行
  ├─ Task 4 ✓ 成功
  ├─ Task 5 ✓ 成功
  ├─ Task 6 ✗ 失败 ← 记录错误，继续执行
  ├─ Task 7 ✓ 成功
  └─ Task 8 ✓ 成功

结果: 6/8 成功，2/8 失败
策略: 继续执行（推荐）
      ↓
返回部分结果（6个成功的分析）

策略: 立即中止
      ↓
抛出错误，终止所有任务
```

## 性能监控

```
┌─────────────────────────────────────────────────────────────┐
│                      监控指标                                │
└─────────────────────────────────────────────────────────────┘

实时指标:
  ├─ 当前进度: 60%
  ├─ 已完成任务: 6/8
  ├─ 正在执行: [Task7, Task8]
  └─ 预计剩余时间: 48秒

历史指标:
  ├─ 总耗时: 240秒
  ├─ 平均单任务耗时: 30秒
  ├─ 缓存命中率: 75%
  ├─ 成功率: 87.5% (7/8)
  └─ 失败任务: [Task3]

缓存统计:
  ├─ 缓存项数: 24
  ├─ 占用空间: 2.3 MB
  └─ 命中次数: 18
```

## 组件交互

```
┌─────────────────────────────────────────────────────────────┐
│                    组件交互图                                │
└─────────────────────────────────────────────────────────────┘

AlpinePanel (UI)
      ↓
      ├─ perfSettings.settings ← 读取配置
      ↓
actions.runAnalysisAction()
      ↓
      ├─ getPerformanceSettings() ← 获取用户设置
      ↓
parallelAnalysisService.runParallelAIAnalysis()
      ↓
      ├─ getCachedResult() ← 检查缓存
      ├─ executeAnalysisTask() ← 执行分析
      ├─ setCachedResult() ← 保存缓存
      └─ onProgress() ← 报告进度
      ↓
aiAnalysisService.callLLM()
      ↓
llmService.callLLM()
      ↓
External API (OpenAI/Claude/etc.)
```

## 配置流程

```
┌─────────────────────────────────────────────────────────────┐
│                  性能设置流程                                │
└─────────────────────────────────────────────────────────────┘

用户点击"性能设置"
      ↓
打开设置模态框
      ↓
      ├─ 显示当前配置
      ├─ 显示缓存统计
      └─ 显示预计效果
      ↓
用户调整设置
      ├─ 拖动并发数滑块
      ├─ 切换缓存开关
      └─ 选择失败策略
      ↓
点击"保存设置"
      ↓
savePerformanceSettings()
      ↓
保存到 localStorage
      ↓
关闭模态框
      ↓
下次分析使用新配置
```

---

**架构特点**：
- 模块化设计
- 职责清晰
- 易于扩展
- 高性能
- 用户友好
