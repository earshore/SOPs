# 产品体验优化梳理：技能页 + Deep Chat 调用 Skill

> 范围：今天尚未 Push 的开发内容 ——「更多 / 大模型探索 / 技能」页，以及「应用中心 / Playground / Deep Chat」通过上下文挂载调用技能页 skill 的链路。
> 视角：产品体验（交互流程 / 界面布局 / 反馈机制 / 可用性 / 一致性），不展开代码缺陷排查，只谈体验优化。
> 锚点：所有结论都对应到真实 UI 与代码行号，便于直接落地。

---

## 一、交互流程优化

### F1【最高优先级】「在 Deep Chat 试用」在已停留 Deep Chat 时静默失效
- **现象**：从技能页点「试用」→ 入队 `queueSkillForDeepChat` → 导航到 Deep Chat → 在 `init()` 内 `consumeSkillForDeepChat()` 消费。但 `init()` 只在模块首次挂载时执行一次。
- **证据**：
  - 消费点仅在 `controller.ts:207-211`（位于 `init()` 内）。
  - 入队/消费即销毁：`skillDeepChatHandoff.ts:25-34`（`consume` 后 `pendingSkillContext = null`）。
  - 技能页点了之后仍弹成功 toast：`skills/index.ts:262` `showToast('正在 Deep Chat 载入技能…', {type:'success'})`。
- **影响**：最常见的使用路径（浏览技能 → 试用 A → 不满意 → 试用 B，全程不离开 Deep Chat）直接断掉；用户看到"正在载入"但对话无任何变化，且无任何错误提示。这是核心 CTA 的体验断点。
- **建议**：
  - 把"消费技能上下文"从 `init()` 移到**路由激活 / 模块可见**的钩子（如 `onResume` 或订阅一个事件总线），让已挂载的 Deep Chat 也能接收。
  - 或：在 `trySkillInDeepChat` 里判断 Deep Chat 是否已挂载，已挂载则直接调用 `createThreadFromSkillContext`，不再依赖 `init` 重跑。
  - 入队前若检测到上一条 `pendingSkillContext` 未消费，给出提示而非静默覆盖。

### F2【中】挂载技能只能"新建会话"，无法"附加到当前会话"
- **现象**：`createThreadFromSkillContext`（`controller.ts:1788-1805`）永远 `createThread` 新建线程。用户若已在某个对话中，想临时加一个方法论，被迫开新会话、丢掉上下文。
- **建议**：技能页「试用」提供两种意图（或 Deep Chat 内提供"把当前技能加入本会话"入口）；至少允许用户从会话内主动挂载技能，而不只从技能页单向触发。

### F3【中】卡片三个无文字图标按钮，主操作发现成本高
- **现象**：技能卡片底部是 `查看 / 复制 / 在 Deep Chat 试用` 三个纯图标按钮（`skills/index.ts:226-236`，分别为 `fa-eye / fa-copy / fa-paper-plane`），无文字标签；整张卡片点击=查看详情。三项操作"平铺"且语义弱（paper-plane 表示"试用"不直观）。
- **建议**：把"在 Deep Chat 试用"作为卡片主操作做成**带文字的实心按钮**；查看/复制降为次要图标。让核心 CTA 一眼可见（详见一致性 C1）。

### F4【低】从技能页跳走后，搜索词/分类被重置
- **现象**：`skills/index.ts:496-503` `unmount()` 把 `currentCategory/currentKeyword` 清零。试用后返回技能页需重新筛选。
- **建议**：用模块级或 Storage 记忆上次筛选状态；或导航回来时恢复。`unmount` 重置是合理的，但"试用"是高频中断场景，记忆筛选体验更好。

---

## 二、界面布局优化

### L1【最高优先级】已挂载技能在主会话区"不可见"
- **现象**：技能原文注入到**隐藏的系统提示词**（`applySkillContextsToSession`，`controller.ts:1815-1825`，写入 `#deep-chat-system-prompt` 这个默认折叠的 textarea）；唯一可见痕迹是输入框草稿里一个内联 Chip（`createInlineSkillChip`，`controller.ts:2008-2042`）。用户看对话本身时，完全不知道当前回答是由哪个方法论驱动的。
- **证据**：线程列表项无技能标识 —— `skillContexts` 在 `controller.ts:2669` 只用于"是否可持久化"判断，未进入列表渲染；`renderers` 列表项也没有技能徽标。
- **影响**：用户无法确认"技能到底挂上没有 / 挂的是哪个"，也易于在多个会话间混淆上下文。
- **建议**：
  - 在**消息区上方**加一条常驻"上下文条"（Context Bar），展示已挂载技能为可移除 Chip，独立于输入框草稿（同时解决 F5 / U2 的误删问题）。
  - 在**左侧会话列表项**为带技能的线程加一个小徽标（如 `fa-graduation-cap`），让用户从列表就能区分"普通对话"与"技能对话"。
  - 上下文条与输入框 Chip 二选一承载"可移除"交互，避免同一信息两处维护。

### L2【中】技能页首屏被说明性内容占满，目录列表被推到下方
- **现象**：首屏依次是 Hero 横幅（`template.html:2-33`）、4 张指标卡（`:35-96`）、"运营怎么用"三步说明（`:98-124`），最后才是"全部技能"目录（`:126-159`）。对一个"浏览/检索"任务，列表应在首屏可见。
- **建议**：
  - 把"运营怎么用"三步与 Hero 描述合并为紧凑页头；指标卡与说明可折叠或下沉。
  - 搜索框 + 分类筛选做成**吸顶（sticky）**，列表上移；目录即首屏主要内容。

### L3【中】卡片 vs 模态的操作呈现不一致（详见 C1）
- 卡片：3 个无文字图标（`skills/index.ts:226-236`）。
- 模态：3 个带文字按钮「关闭 / 在 Deep Chat 试用 / 复制全文到剪贴板」（`template.html:205-227`）。
- 同一组操作两种视觉语言，建议统一为"主操作带文字 + 次要图标"。

### L4【低】技能详情正文为单一长 `<pre>`，无结构化预览
- **现象**：模态正文直接 `skill.raw` 整段塞进 `<pre>`（`skills/index.ts:334`），长文无分段/折叠。
- **建议**：利用已有的 SKILL.md 解析能力，抽取"能力边界 / 输入要求 / 输出格式"做结构化区块；长文可折叠"查看完整原文"。降低阅读负担。

---

## 三、反馈机制优化

### FB1【高】跨页跳转 + 异步挂载，缺少"过渡中"状态
- **现象**：技能页点「试用」→ `navigateToRouteId` 异步导航 → Deep Chat `init()` 内还要 `await loadThreadStore()` / `await ensureDeepChatElementDefined()` 才建线程。这段间隙无 loading；且若命中 F1（已挂载），连"失败"都不提示。
- **建议**：技能页点击后立即给明确的"正在打开 Deep Chat 并载入技能"过渡态（按钮 loading / 进度提示）；Deep Chat 侧在建技能线程时显示短暂横幅「正在载入技能「X」」；若消费失败（如已挂载场景）给出可操作的错误而非静默。

### FB2【中】系统提示词被静默覆盖，无确认/无提示
- **现象**：`applySkillContextsToSession`（`controller.ts:1815-1825`）直接用技能原文**覆盖** `sessionSystemPrompt`，并写入隐藏 textarea。若用户此前手动填过系统提示词，内容被替换且无任何提示。
- **建议**：
  - 若已有非空系统提示词，挂载前**确认**（合并 or 覆盖）。
  - 挂载后在可见位置给一条反馈「系统提示词已更新为「X」方法论」（可放在 L1 的上下文条里）。
  - 提供"查看/编辑当前系统提示词"的显眼入口，而不是藏在折叠的 tuning 面板。

### FB3【低】移除技能上下文无撤销
- **现象**：`dismissInlineSkillContext`（`controller.ts:2044-2069`）移除后仅 toast「已移除技能上下文」，`showToast` 无 undo。
- **建议**：轻量撤销（3-5s 内可恢复），降低误操作成本。

### FB4【中，正向】空状态与复制反馈已有基础，可补强
- **正向**：搜索无结果有引导空态（`skills/index.ts:292-303`，建议清空搜索/切全部/关键词示例）；复制有成功/失败 toast（`:310-316`）。
- **补强**：分类 + 搜索组合无结果时，提示"是否清空筛选"；技能库为空时整页（含 Hero 指标）应有一致引导，而非仅列表区空态、Hero 仍显示 0。

---

## 四、可用性优化

### U1【最高优先级】边界场景：已停留 Deep Chat 时「试用」失败（即 F1）
- 同上，这是最致命的可用性断点，核心路径在最常见场景下不可用。

### U2【中】输入框内联 Chip 在 contenteditable 中易被破坏
- **现象**：Chip 是 `contenteditable=false` 的 `<span>`，与可编辑草稿混排（`controller.ts:2008-2042`）。用户编辑草稿时容易删掉或手改「技能名」文字，`normalizeSkillChipDraftText`（`skillDeepChatHandoff.ts:50-69`）尝试修复但无法保证；一旦匹配不到，挂载在序列化后丢失，且用户无感知。
- **建议**：将"已挂载技能"移出可编辑区（见 L1 上下文条），输入框只保留纯业务草稿，从根本上消除误改风险。

### U3【中】会话恢复后，若输入 Chip hydrate 失败则"挂载存在但不可见"
- **现象**：`hydrateActiveThreadInlineSkillChips`（`controller.ts:1950-1971`）对 shadow DOM 未就绪做有限重试；若最终失败，线程仍带 `skillContexts`（系统提示词生效），但输入框无 Chip、主会话区也无标识 → 状态不一致。
- **建议**：挂载状态应有**不依赖输入框 hydrate** 的可靠来源（即 L1 的上下文条从 `thread.skillContexts` 直接渲染），保证"挂了就一定看得见"。

### U4【低】窄屏下三栏布局未做响应式降级
- **现象**：`styles.css:43-66` 固定 `grid-template-columns: minmax(220,240) 1fr minmax(240,280)`，仅 thread rail 有折叠态（`is-rail-collapsed`），prompt rail 无；小屏/平板下主对话区被挤压。
- **建议**：窄屏时 prompt rail 自动收起或改为抽屉；提供统一的"唤起上下文/历史"入口。

### U5【低】键盘可达性基本到位，可锦上添花
- **正向**：卡片 Enter/Space 打开（`skills/index.ts:396-405`），模态 Esc 关闭（`:446-450`），Chip 有 `aria-label`（`:2029`）。
- **建议**：卡片内三个操作按钮在键盘流中的可达顺序与 Tab 顺序一致性可再核对；Chip 的 dismiss 按钮 `tabIndex=-1` 导致键盘无法直接聚焦移除，建议允许 Tab 聚焦（配合可见焦点环）。

---

## 五、一致性优化

### C1【中】卡片与模态的操作呈现不一致
- **现象**：卡片=3 无文字图标；模态=3 带文字按钮。同一组操作两套视觉语言。
- **建议**：统一为「主操作（在 Deep Chat 试用）带文字实心按钮 + 次要操作（查看/复制）图标」，卡片与模态共用同一组件/模板。

### C2【中】术语不统一
- **现象**：
  - 状态：卡片显示 `Available / Beta`（`skills/index.ts:54-64`），但指标卡称 Beta 为"试用版技能"（`:93-94`），Hero 称"全文可复制 / 中文外壳·英文原文"。
  - "试用"一词既指 Beta 状态，又指"在 Deep Chat 试用"动作，易混。
- **建议**：建立术语表 —— 状态用统一标签（如"正式 / 试用"），动作统一叫"在 Deep Chat 试用"；指标卡与卡片状态文案对齐。

### C3【中】两个联动功能的主题色割裂
- **现象**：技能页是**紫/品红**主题（`wb-theme-violet`，`template.html:3`）；Deep Chat 是**棕/赤陶**主题（`--deep-chat-accent:#a85f3f`，`styles.css:1-13`）。一个意图连续的操作（紫页 → 棕对话）发生明显的色彩断层，削弱"这是同一件事"的认知。
- **建议**：技能→对话这条链路共享一个强调色（或至少"技能上下文"Chip/徽标在两端用同一色），让上下文挂载在视觉上被识别为同一对象。

### C4【低】图标隐喻不统一
- **现象**：技能"试用"用 `fa-paper-plane`（发送），Deep Chat 自身用 `fa-wand-magic-sparkles`（AI 魔法）。同一"用 AI 试技能"动作，两端隐喻不同。
- **建议**：链路统一一个"试技能"图标（如 `fa-graduation-cap` 或 `fa-wand-magic-sparkles`），强化关联。

### C5【低，正向】Toast 文案风格基本统一
- 技能页与 Deep Chat 都用「已附加技能「X」」句式，保持了一致，建议延续该风格到所有跨页反馈。

---

## 优先级总排序（给 Grok 的落地顺序）

| 优先级 | 编号 | 优化项 | 维度 | 一句话收益 |
|---|---|---|---|---|
| **P0** | F1 / U1 | 已停留 Deep Chat 时「试用」静默失效 | 流程/可用性 | 修好核心 CTA 在最常见路径下的可用性 |
| **P0** | L1 | 已挂载技能在主会话区/会话列表不可见 | 布局 | 让用户随时确认"挂了什么、挂没挂上" |
| **P1** | FB2 | 系统提示词被静默覆盖无提示/无确认 | 反馈 | 避免用户辛苦写的提示词被无声替换 |
| **P1** | U2 / U3 | 输入 Chip 易误删 + 恢复后可能不可见 | 可用性 | 挂载状态有可靠、不依赖输入框的来源 |
| **P1** | C3 | 技能页(紫)与 Deep Chat(棕)主题割裂 | 一致性 | 强化"同一件事"的视觉连贯 |
| **P1** | F3 / C1 | 卡片无文字图标 vs 模态带文字，主操作弱 | 流程/一致性 | 核心 CTA 一眼可见、两处统一 |
| **P2** | L2 | 首屏说明过多，目录被推下 | 布局 | 浏览任务首屏即见列表 |
| **P2** | FB1 | 跨页跳转缺过渡态 | 反馈 | 异步挂载过程有清晰状态 |
| **P2** | C2 | 术语不统一（Beta/试用/可用） | 一致性 | 降低理解成本 |
| **P2** | F2 | 只能新建会话挂载，不能附加当前 | 流程 | 支持"向现有对话加技能" |
| **P3** | FB3 | 移除无撤销 | 反馈 | 降低误操作成本 |
| **P3** | L4 | 详情正文无结构化预览 | 布局 | 长文阅读减负 |
| **P3** | U4 | 窄屏三栏未降级 | 可用性 | 平板/小屏可用 |
| **P3** | C4 / U5 | 图标隐喻、Chip 键盘可达 | 一致性/可用性 | 细节打磨 |
| **P3** | F4 / FB4 / L3(部分) | 筛选记忆、空状态组合提示、卡片模态统一收尾 | 流程/反馈/一致性 | 收尾打磨 |

---

## 落地建议（给 Grok）
1. **先做 P0 两项**：F1 改消费时机 + L1 加上下文条，这两项不动就谈不上"技能可在 Deep Chat 用"的完整体验。
2. P1 里 **FB2（系统提示词覆盖确认）** 和 **U2/U3（Chip 迁出输入框）** 可与 L1 的"上下文条"一并重构——把可移除交互集中到上下文条，输入框回归纯草稿，一举解决 3 个问题。
3. 一致性（C3 主题、C1/C2 文案与操作）建议在 P0/P1 结构定下来后统一收口，避免反复改样式。
4. 所有反馈文案沿用现有「已附加技能「X」」风格，保持跨页一致。
